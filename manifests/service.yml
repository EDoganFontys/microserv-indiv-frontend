apiVersion: v1
kind: Service
metadata:
    name: "emrekubernetes-f107"
    labels:
        app: "emrekubernetes-f107"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "emrekubernetes-f107"