apiVersion: v1
kind: Service
metadata:
    name: "emrekubernetes-384e"
    labels:
        app: "emrekubernetes-384e"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "emrekubernetes-384e"